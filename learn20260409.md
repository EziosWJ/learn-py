# 关于python的异步编程注意事项和理解差异

1. 关于思维：放心用 JS 的思维去理解 Python 的异步。把 await aiomysql.connect() 想象成 JS 里的 await fetch()，完全没问题。
2. 关于写法：记住 Python 没有“顶层 await”，所有异步代码必须包在 async def 里。
3. 关于第三方库：在 Python 里看到 import 了一个库，只要没带 aio 前缀，使用它时绝对不要加 await，加了就报错；而带了 aio 的库，用的时候必须加 await，不加就不执行。